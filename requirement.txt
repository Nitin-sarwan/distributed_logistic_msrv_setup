# Runtime dependencies
alembic==1.19.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
fastapi==0.141.1
idna==3.18
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.13.0
pydantic_core==2.46.4
sqlalchemy==2.0.44
starlette==1.6.0
typing-inspection==0.4.3
typing_extensions==4.16.0
uvicorn[standard]==0.52.1

# Email validation — required by pydantic's EmailStr
email-validator==2.3.0

# Password hashing
bcrypt==5.0.0

# AES-256-CBC token cipher, kept compatible with the Node service
cryptography==50.0.0

# Shared session store. As of 4.x pymongo depends on dnspython directly, so
# mongodb+srv:// URIs resolve without the (now removed) [srv] extra.
pymongo==4.17.0

# Gateway -> service HTTP forwarding
httpx==0.28.1
