# Local dev (no server required)
DATABASE_URL=sqlite:///./dev.db

# Postgres (production) - requires: pip install "psycopg[binary]"
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/mydb
